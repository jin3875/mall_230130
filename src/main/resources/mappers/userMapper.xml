<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.user.dao.UserDAO">
	<!-- TEST -->
	<select id="selectUserListTEST" resultType="map">
		SELECT
			*
		FROM
			`user`
	</select>
	
	<!-- 아이디 존재 유무 -->
	<select id="existLoginId" parameterType="String" resultType="boolean">
		SELECT
			COUNT(*)
		FROM
			`user`
		WHERE
			`loginId` = #{loginId}
	</select>
	
	<!-- 유저 추가 -->
	<insert id="insertUser" parameterType="map">
		INSERT INTO `user`
		(
			`loginId`
			,`password`
			,`name`
			,`phoneNumber`
			,`email`
			,`postcode`
			,`address`
			,`detailAddress`
			,`type`
			,`createdAt`
			,`updatedAt`
		)
		VALUES
		(
			#{loginId}
			, #{password}
			, #{name}
			, #{phoneNumber}
			, #{email}
			, #{postcode}
			, #{address}
			, #{detailAddress}
			, 0
			, NOW()
			, NOW()
		)
	</insert>
	
	<!-- 유저 검색 -->
	<select id="selectUserByLoginIdPassword" parameterType="map" resultType="com.mall.user.model.User">
		SELECT
			`id`
			,`loginId`
			,`password`
			,`name`
			,`phoneNumber`
			,`email`
			,`postcode`
			,`address`
			,`detailAddress`
			,`type`
			,`createdAt`
			,`updatedAt`
		FROM
			`user`
		WHERE
			`loginId` = #{loginId}
			AND `password` = #{password}
	</select>
	
	<!-- 아이디 검색 -->
	<select id="selectUserByNamePhoneNumber" parameterType="map" resultType="com.mall.user.model.User">
		SELECT
			`id`
			,`loginId`
			,`password`
			,`name`
			,`phoneNumber`
			,`email`
			,`postcode`
			,`address`
			,`detailAddress`
			,`type`
			,`createdAt`
			,`updatedAt`
		FROM
			`user`
		WHERE
			`name` = #{name}
			AND `phoneNumber` = #{phoneNumber}
	</select>
</mapper>